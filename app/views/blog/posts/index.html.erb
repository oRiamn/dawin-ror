<ul>
<%@posts.each do |post| %>
	<div>
		<%=link_to [:blog, post] do%>
				<%=image_tag post.picture.thumb.url%> 
				<br/>
				<h1 style="display:inline;"><%=post.title%></h1>
				<h2 style="display:inline;"> (published on the <%=post.publishDate.strftime('%d/%m/%Y at %H:%M')%>)</h2>
				<p><%=post.header%></p> 
				<p>By <%=post.user.fullname%></p>
		<%end%>
		<%if current_user.present? && post.user_id == current_user.id %>
			<%=link_to "Edit", edit_blog_post_path(post)%>
			<%=link_to "Delete", [:blog, post], method: :delete%>
		<%end%>
	</div>
<%end%>
</ul>
<%=link_to "New Post", [:new, :blog, :post]%>